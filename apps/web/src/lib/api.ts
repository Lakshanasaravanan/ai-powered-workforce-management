const base=import.meta.env.VITE_API_BASE_URL ?? 'http://localhost:8001';type Employee={id:string;employee_code:string;full_name:string;company_email:string;role:string;designation:string;department:string;manager_id:string|null;is_active:boolean};async function request<T>(path:string,init:RequestInit={}){const r=await fetch(base+path,{...init,headers:{'Content-Type':'application/json','Authorization':'Bearer '+sessionStorage.getItem('infotech_token'),...init.headers}});if(!r.ok)throw Error(await r.text());return r.json() as Promise<T>}export const employees={list:()=>request<Employee[]>('/api/v1/employees'),search:(q:string)=>request<Employee[]>('/api/v1/employees/search?q='+encodeURIComponent(q)),create:(body:object)=>request<Employee&{temporary_password:string}>('/api/v1/employees',{method:'POST',body:JSON.stringify(body)}),update:(id:string,body:object)=>request<Employee>('/api/v1/employees/'+id,{method:'PATCH',body:JSON.stringify(body)})};export type{Employee};
